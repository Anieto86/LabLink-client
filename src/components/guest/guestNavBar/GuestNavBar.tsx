import { Row } from "@/components/design/Grid"

const GuestNavBar = () => {
  return (
    <Row className="w-full min-h-10 bg-blue-500 items-center justify-center">
      GuestNavbar
    </Row>
  )
}

export default GuestNavBar